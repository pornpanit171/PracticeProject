# ตั้งค่า Google Sheets สำหรับเก็บประวัติ

ทำครั้งเดียวโดยเจ้าของระบบ ใช้เวลาประมาณ 10 นาที
**คนที่มาทำข้อสอบไม่ต้องทำอะไรในหน้านี้เลย** แค่เปิดลิงก์แล้วทำข้อสอบ

ถ้ายังไม่อยากตั้งค่าตอนนี้ก็ข้ามไปได้ ระบบจะทำงานในโหมดเก็บเฉพาะเครื่องพร้อมปุ่ม Export JSON
แต่ประวัติจะหายถ้าล้างข้อมูลเบราว์เซอร์ และดูข้ามเครื่องไม่ได้

---

## ขั้นตอน

### 1. สร้างสเปรดชีตเปล่า

ไปที่ [sheets.new](https://sheets.new) แล้วตั้งชื่อไฟล์ เช่น `CISSP Quiz History`
ไม่ต้องสร้างแท็บหรือหัวคอลัมน์ใด ๆ สคริปต์จะสร้างให้เอง

### 2. วางโค้ด Apps Script

ในสเปรดชีตนั้น เลือกเมนู **Extensions → Apps Script**
ลบโค้ดตัวอย่างในไฟล์ `Code.gs` ทิ้งทั้งหมด แล้ววางเนื้อหาจากไฟล์ [`apps-script/Code.gs`](apps-script/Code.gs) ของ repo นี้ลงไปแทน
กด **Save** (ไอคอนแผ่นดิสก์)

### 3. รันฟังก์ชัน `setup` หนึ่งครั้ง

บนแถบด้านบนของ Apps Script เลือกฟังก์ชัน **setup** จาก dropdown แล้วกด **Run**

ครั้งแรก Google จะขอสิทธิ์ — กด **Review permissions** → เลือกบัญชีของคุณ →
หน้าจอจะเตือนว่า "Google hasn't verified this app" ให้กด **Advanced** → **Go to … (unsafe)** → **Allow**
(คำเตือนนี้เป็นเรื่องปกติสำหรับสคริปต์ที่เขียนเอง เพราะไม่ได้ผ่านการรีวิวจาก Google)

เมื่อรันเสร็จจะมีกล่องข้อความแสดง **sheetsKey** ให้ก๊อปเก็บไว้
และในสเปรดชีตจะมี 4 แท็บใหม่: `Attempts` `Answers` `Codes` `Meta`

### 4. Deploy เป็น Web App

กด **Deploy → New deployment** → กดไอคอนเฟือง เลือก **Web app** แล้วตั้งค่า:

| ช่อง | ค่าที่ต้องเลือก |
|---|---|
| Description | อะไรก็ได้ เช่น `v1` |
| Execute as | **Me (อีเมลของคุณ)** |
| Who has access | **Anyone** |

> **Who has access ต้องเป็น Anyone** ไม่ใช่ "Anyone with Google account"
> ไม่งั้นคนที่ไม่ได้ล็อกอิน Google จะส่งผลขึ้นชีตไม่ได้
> ข้อแลกคือใครที่รู้ URL นี้ก็ยิงเข้าได้ จึงมีคีย์ `sheetsKey` กันไว้อีกชั้น

กด **Deploy** แล้วก๊อป **Web app URL** ที่ลงท้ายด้วย `/exec`

### 5. ใส่ค่าลง `assets/config.js`

```js
sheetsUrl: 'https://script.google.com/macros/s/AKfycb..../exec',
sheetsKey: 'คีย์ที่ได้จากขั้นตอนที่ 3',
```

commit แล้ว push — เสร็จเรียบร้อย

### 6. ตรวจว่าใช้ได้จริง

เปิดเว็บ ทำข้อสอบสัก 2-3 ข้อจนจบ แล้วดูมุมซ้ายบน:

- **"ซิงค์ขึ้น Google Sheets แล้ว"** = ใช้ได้
- **"รอซิงค์อยู่ 1 รายการ"** = ติดต่อชีตไม่ได้ กด "ลองใหม่" แล้วเช็คว่า URL กับคีย์ถูกต้อง และ Deploy ตั้งเป็น Anyone
- **"เก็บเฉพาะในเครื่อง"** = ยังไม่ได้ใส่ `sheetsUrl` ใน config

---

## หน้าตาข้อมูลในสเปรดชีต

### แท็บ `Attempts` — 1 แถวต่อการทำข้อสอบ 1 ครั้ง

| คอลัมน์ | ความหมาย |
|---|---|
| `attempt_id` | รหัสอ้างอิงของครั้งนั้น ใช้เชื่อมกับแท็บ Answers |
| `timestamp` | เวลาที่ข้อมูลเข้าชีต |
| `name` | ชื่อผู้ทำ |
| `mode` / `order` / `reveal` | practice หรือ exam / sequential หรือ random / instant หรือ end |
| `domains` | โดเมนที่ปรากฏจริงในชุดนั้น |
| `total` `correct` `wrong` `unanswered` | จำนวนข้อ |
| `score_pct` | คะแนนเป็นเปอร์เซ็นต์ |
| `duration_sec` | เวลาที่ใช้ทั้งหมด |
| `bank_version` | เวอร์ชันคลังข้อสอบตอนที่ทำ ใช้ดูว่าทำก่อนหรือหลังแก้โจทย์ |

### แท็บ `Answers` — 1 แถวต่อ 1 ข้อที่ตอบ

`attempt_id` `seq` `qid` `domain` `picked` `is_correct` `time_ms` `flagged`

### แท็บ `Codes` — รหัสเข้าใช้งาน (มีผลเมื่ออัปเป็นระบบรหัสระดับ 3)

| code | name | active | expires | max_uses | used | note |
|---|---|---|---|---|---|---|
| `CISSP-2026` | *(ว่าง)* | TRUE | | | 0 | รหัสกลาง ผู้ใช้พิมพ์ชื่อเอง |
| `CISSP-7K2M` | สมชาย | TRUE | 2026-12-31 | 50 | 12 | รหัสรายคน ล็อกชื่อให้ |

- `name` ว่าง = รหัสกลาง / `name` มีค่า = รหัสรายคน
- `active` เป็น `FALSE` = ปิดรหัสนั้นทันที
- `expires` และ `max_uses` เว้นว่าง = ไม่จำกัด
- `used` ระบบนับให้เอง ไม่ต้องกรอก

### แท็บ `Meta`

เก็บ `apiKey` (คีย์กันสแปม) และ `encKey` (กุญแจ AES สำหรับระบบรหัสระดับ 3)

---

## วิเคราะห์ผลด้วย Pivot Table

จุดที่คุ้มที่สุดของการเก็บลง Sheets คือทำ pivot ได้ทันทีโดยไม่ต้องเขียนโค้ดเพิ่ม

**ดูว่าโดเมนไหนอ่อน:** เลือกช่วงในแท็บ `Answers` → Insert → Pivot table
- Rows: `domain`
- Values: `is_correct` แบบ COUNTA และแบบ COUNTIF ของ TRUE
- จะเห็นอัตราตอบถูกรายโดเมนทันที

**ดูพัฒนาการตามเวลา:** ใช้แท็บ `Attempts`
- Rows: `timestamp` (จัดกลุ่มเป็นรายวัน/สัปดาห์)
- Values: ค่าเฉลี่ยของ `score_pct`
- แล้ว Insert → Chart เป็นกราฟเส้น

**หาข้อที่ใช้เวลานานผิดปกติ:** แท็บ `Answers`
- Rows: `qid`
- Values: ค่าเฉลี่ย `time_ms` เรียงมากไปน้อย

---

## เรื่องที่ควรรู้

**URL ของ Web App เป็นความลับระดับ "อย่าเอาไปโพสต์"**
เมื่อ commit ลง repo สาธารณะแล้ว ใครเจอก็ยิงได้ สิ่งที่ป้องกันไว้คือ:
สคริปต์ไม่มีคำสั่งลบหรือแก้แถวเดิม (append อย่างเดียว) จำกัดจำนวนข้อต่อครั้ง
และถ้า `attempt_id` ซ้ำจะไม่เขียนซ้ำ

**ถ้าเจอสแปมหรือรหัสรั่ว** ให้ Deploy เวอร์ชันใหม่ (Deploy → Manage deployments → แก้ไข → New version)
จะได้ URL ใหม่ ของเก่าใช้ไม่ได้ทันที แล้วเปลี่ยน `sheetsUrl` ใน config.js บรรทัดเดียว

**โควตา Apps Script** ของบัญชี Google ฟรีคือรันสคริปต์รวมได้ 90 นาทีต่อวัน
การบันทึกผลหนึ่งครั้งใช้เวลาไม่ถึงวินาที จึงรองรับการใช้งานหลักพันครั้งต่อวันได้สบาย

**แก้โค้ดแล้วต้อง Deploy ใหม่เสมอ** การกด Save ใน Apps Script ไม่ทำให้ Web App เปลี่ยนตาม
ต้องไป Deploy → Manage deployments → Edit → Version: New version → Deploy
