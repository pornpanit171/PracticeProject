/**
 * config.js — ค่าตั้งต้นของระบบ แก้ไฟล์นี้ไฟล์เดียวก็เปลี่ยนพฤติกรรมได้ทั้งระบบ
 * ไฟล์นี้ commit ขึ้น GitHub ตามปกติ ห้ามใส่ความลับจริงลงไป
 */
window.APP_CONFIG = {
  /* ---------- ระบบรหัสเข้าใช้งาน ---------- */
  // 1 = ตรวจรหัสในเบราว์เซอร์ด้วย hash ใน data/codes.json (กันคนบังเอิญเจอ ไม่กันคนตั้งใจ)
  // 3 = ส่งรหัสไปตรวจที่ Apps Script แล้วรับกุญแจถอดรหัสกลับมา (เพิกถอนรหัสได้)
  authLevel: 1,

  // เปิดประตูรหัสหรือไม่ — ตั้ง false เพื่อให้ใครก็เข้าทำข้อสอบได้เลย
  authRequired: true,

  // true เมื่อไฟล์ใน data/ ถูกเข้ารหัสไว้แล้ว (ใช้คู่กับ authLevel 3)
  encrypted: false,

  /* ---------- Google Sheets (ประวัติการทำข้อสอบ) ---------- */
  // วาง URL ที่ลงท้ายด้วย /exec ที่ได้จากการ Deploy Apps Script — ดูขั้นตอนใน SHEETS_SETUP.md
  // เว้นว่างไว้ได้ ระบบจะทำงานในโหมดเก็บเฉพาะเครื่องพร้อมปุ่ม Export
  sheetsUrl: '',

  // คีย์สุ่มที่ได้จากการรัน setup() ใน Apps Script — กันสแปมจากคนที่ไม่มีลิงก์เว็บนี้
  sheetsKey: '',

  /* ---------- ค่าเริ่มต้นของการทำข้อสอบ ---------- */
  defaultCount: 50,          // จำนวนข้อเริ่มต้นในหน้า Setup
  secondsPerQuestion: 90,    // เวลาต่อข้อ (วินาที) ตั้ง 0 เพื่อปิดการจับเวลา
  passMark: 70,              // เกณฑ์ผ่าน (%) ใช้แสดงผลบนหน้าสรุปเท่านั้น
  shuffleChoices: false,     // true = สลับลำดับตัวเลือกในแต่ละข้อด้วย

  /* ---------- อื่น ๆ ---------- */
  defaultLang: 'en',         // 'en' | 'th' | 'both'
  storageNamespace: 'cissp-bank-v1'
};
